{
  "name": "UI-Butler-turborepo",
  "private": true,
  "scripts": {
    "build": "turbo build:types --parallel --concurrency=2 & turbo bootstrap --parallel --concurrency=4 & turbo build --parallel --concurrency=20",
    "start": "turbo start --parallel --concurrency=20",
    "start:frontend": "turbo start:frontend --parallel --concurrency=5",
    "start:backend": "turbo start:backend --parallel --concurrency=10",
    "dev": "turbo build:types --parallel --concurrency=2 & turbo bootstrap --parallel --concurrency=4 & turbo dev --parallel --concurrency=20",
    "lint": "turbo build:types --parallel --concurrency=2 & turbo bootstrap --parallel --concurrency=4 &turbo lint --parallel --concurrency=20",
    "type-check": "turbo build:types --parallel --concurrency=2 & turbo bootstrap --parallel --concurrency=4 & turbo type-check  --parallel --concurrency=20",
    "bootstrap": "turbo build:types --parallel --concurrency=2 & turbo bootstrap --parallel --concurrency=4",
    "clean": "turbo clean --parallel --concurrency=20 & rimraf node_modules",
    "clean:cache": "rimraf .turbo",
    "format": "prettier --write \"**/*.{js,jsx,json,ts,tsx,scss,css,md}\"",
    "prepare": "husky",
    "lint-staged": "lint-staged"
  },
  "devDependencies": {
    "eslint-import-resolver-typescript": "^3.7.0",
    "eslint-plugin-import": "^2.31.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.4.3",
    "prettier": "^3.5.0",
    "prettier-plugin-tailwindcss": "^0.6.11",
    "turbo": "^2.4.0"
  },
  "packageManager": "pnpm@10.2.1",
  "engines": {
    "node": ">=22"
  }
}
